import React from "react";

const Navbar = () => {
  return (
    <nav className="navbar navbar-dark bg-primary p-3">
      <span className="navbar-brand mx-auto">Student Dashboard</span>
    </nav>
  );
};

export default Navbar;
